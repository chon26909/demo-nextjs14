import React from 'react';
import InputOTP from './InputOTP';

type Props = {};

const page = (props: Props) => {
    return (
        <div className='p-10'>
            <InputOTP />
        </div>
    );
};

export default page;
